@model IEnumerable<VOTACIONES.Models.Aprendiz>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.correo_aprendiz)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.contraseña_aprendiz)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.correo_aprendiz)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.contraseña_aprendiz)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_aprendiz }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_aprendiz }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_aprendiz })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
