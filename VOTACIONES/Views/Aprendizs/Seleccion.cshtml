
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Seleccionar Jornada</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f4f6f5;
      text-align: center;
      padding: 40px 20px;
    }

    h1 {
      margin-bottom: 30px;
      font-weight: bold;
      color: #004d26;
    }

    .boton-jornada, .btn-sena {
      width: 100%;
      max-width: 400px;
      margin: 10px auto;
      padding: 14px 20px;
      background-color: #006633;
      color: white;
      border: none;
      border-radius: 10px;
      font-size: 18px;
      transition: all 0.3s ease;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

    .boton-jornada:hover, .btn-sena:hover {
      background-color: #004d26;
      transform: scale(1.03);
    }

    /* Modal */
    .modal {
      display: none;
      position: fixed;
      z-index: 9999;
      padding: 10px;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      overflow-y: auto;
    }

    .modal-contenido {
      background-color: white;
      margin: 10% auto;
      padding: 25px 30px;
      border-radius: 15px;
      max-width: 500px;
      box-shadow: 0 12px 24px rgba(0,0,0,0.2);
      animation: aparecer 0.4s ease-out;
    }

    keyframes aparecer {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .cerrar {
      float: right;
      font-size: 24px;
      font-weight: bold;
      color: #999;
      cursor: pointer;
    }

    .cerrar:hover {
      color: #000;
    }

    ul {
      text-align: left;
      padding-left: 20px;
    }

    media (max-width: 600px) {
      h1 {
        font-size: 24px;
      }

      .boton-jornada, .btn-sena {
        font-size: 16px;
        padding: 12px;
      }
    }
    </style>
</head>
<body>

    <h1>Seleccione su jornada</h1>

    <button class="btn-sena" onclick="mostrarInstrucciones()">📝 Ver instrucciones</button>

    <button class="boton-jornada" onclick="location.href='@Url.Action("Diurna", "Aprendizs")'">
        🌞 Jornada Diurna (6:00 am - 12:00 pm)
    </button>

    <button class="boton-jornada" onclick="location.href='@Url.Action("Mixta", "Aprendizs")'">
        🌓 Jornada Mixta (6:00 am - 6:00 pm)
    </button>

    <button class="boton-jornada" onclick="location.href='@Url.Action("Nocturna", "Aprendizs")'">
        🌙 Jornada Nocturna (7:00 pm - 12:00 am)
    </button>


    <!-- Modal -->
    <div id="modalInstrucciones" class="modal" role="dialog" aria-modal="true" aria-labelledby="tituloInstrucciones">
        <div class="modal-contenido">
            <span class="cerrar" onclick="cerrarModal()" aria-label="Cerrar instrucciones">&times;</span>
            <h2 id="tituloInstrucciones" class="mb-3">🗳️ Instrucciones de Votación</h2>
            <p class="text-start">Antes de emitir tu voto, por favor ten en cuenta lo siguiente:</p>
            <ul>
                <li>✅ Solo puedes votar una vez.</li>
                <li>📧 Usa tu correo institucional soy.sena.edu.co o tu número de documento para ingresar.</li>
                <li>🔐 Tu contraseña será tu número de cédula.</li>
                <li>⏱️ La votación estará habilitada hasta las <strong>6:00 PM</strong>.</li>
            </ul>
            <button class="btn-sena mt-3" onclick="cerrarModal()">Entendido</button>
        </div>
    </div>

    <!-- Scripts -->
    <script>
        function mostrarInstrucciones() {
            document.getElementById('modalInstrucciones').style.display = 'block';
        }

        function cerrarModal() {
            document.getElementById('modalInstrucciones').style.display = 'none';
        }



        // Cerrar modal si se hace clic fuera
        window.onclick = function (event) {
            const modal = document.getElementById('modalInstrucciones');
            if (event.target == modal) {
                modal.style.display = "none";
            }
        }
    </script>

</body>
</html>

