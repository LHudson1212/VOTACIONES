@model IEnumerable<VOTACIONES.Models.Votos>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaVoto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aprendiz.correo_aprendiz)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Postulados.Nombre)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaVoto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aprendiz.correo_aprendiz)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Postulados.Nombre)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IdVoto }) |
                @Html.ActionLink("Details", "Details", new { id=item.IdVoto }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IdVoto })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
